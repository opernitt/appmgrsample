import QtQuick 2.0

// A sample UI (AppStore), to be completed
Item {
    Rectangle {
        anchors.fill: parent
        color: "blue"

        Text {
            text: qsTr("AppStore")
            anchors.centerIn: parent
        }
    }
}
