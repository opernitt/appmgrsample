import QtQuick 2.0

// A simple Settings screen, to be completed
// Signals will be used to indicate changes
// to the Controller on the C++ side.
Item {
    Text {
        text: qsTr("Settings")
        anchors.centerIn: parent
    }
}
